{
  "name": "@web2ai/schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ui-scene@1 — the single source of truth exchanged between the Chrome capture side and the Illustrator render side.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./config": "./src/config.ts",
    "./json-schema": "./src/json-schema.ts"
  },
  "scripts": {
    "build": "node --experimental-strip-types src/emit-json-schema.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.1.13"
  }
}
